# AO Chats Mobile — Environment Templates
#
# Development: copy .env.development.example → .env.development
# Production:  set EXPO_PUBLIC_* in EAS and Vercel (see .env.production.example)
#
# All API requests read from EXPO_PUBLIC_* variables at build/runtime.
# Production builds never use localhost.
